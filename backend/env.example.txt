# Primis Backend Environment Variables
# Rename this file to .env and fill in your values

# Server
PORT=3001

# Database (PostgreSQL)
# Get a free database from Supabase (supabase.com) or Railway (railway.app)
DATABASE_URL=postgresql://user:password@host:5432/primis

# Privy (for auth verification)
PRIVY_APP_ID=your-privy-app-id
PRIVY_APP_SECRET=your-privy-app-secret

# Frontend URLs (for CORS)
FRONTEND_URL=http://localhost:5173

# RunPod API (for GPU provisioning)
RUNPOD_API_KEY=your-runpod-api-key

# RunPod Serverless Endpoint (for batch image generation)
# Deploy an SDXL endpoint at https://www.runpod.io/console/serverless
# Then copy the Endpoint ID here
RUNPOD_SDXL_ENDPOINT=your-endpoint-id

# Supabase Storage (for batch results)
# Get these from your Supabase project settings
# SUPABASE_URL is auto-detected from DATABASE_URL
SUPABASE_SERVICE_KEY=your-service-role-key

# Optional: Stripe (for credit purchases)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
