# Nixpacks configuration for Railway
# Specifies Node.js 20 as required by dependencies

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = []

[start]
cmd = "node src/index.js"
